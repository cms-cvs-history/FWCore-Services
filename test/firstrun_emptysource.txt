# Configuration file for EmptyInputService

process TEST = {

	path p = { print }

	module print = AsciiOutputModule { }
	
	source = EmptyInputService {untracked int32 maxEvents = 20
	  untracked uint32 firstRun = 100
	}
}
